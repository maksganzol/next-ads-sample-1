# Новостной портал

Современный новостной портал на Next.js с поддержкой рекламных зон. Проект создан для демонстрации интеграции рекламы в новостные сайты.

## Возможности

- 📰 Современный дизайн новостного портала
- 🎨 Адаптивная верстка (мобильные, планшеты, десктоп)
- 🖼️ Несколько секций: главная новость, сетка новостей, категории
- 📢 Интегрированные рекламные зоны различных размеров
- ⚡ Статическая генерация для быстрой загрузки
- 🚀 Одним скриптом деплоится на GitHub Pages

## Структура проекта

```
.
├── app/                    # Next.js App Router
│   ├── layout.tsx         # Корневой layout
│   ├── page.tsx           # Главная страница
│   └── globals.css        # Глобальные стили
├── components/            # React компоненты
│   ├── Header.tsx         # Шапка сайта
│   ├── Hero.tsx           # Главная новость
│   ├── NewsGrid.tsx       # Сетка новостей
│   ├── NewsCard.tsx       # Карточка новости
│   ├── Categories.tsx     # Категории
│   ├── AdZone.tsx         # Рекламная зона
│   └── Footer.tsx         # Футер
├── lib/                   # Утилиты и данные
│   └── mockNews.ts        # Моковые данные новостей
├── public/                # Статические файлы
│   └── .nojekyll          # Для GitHub Pages
└── scripts/               # Скрипты
    └── deploy.sh          # Скрипт деплоя
```

## Быстрый старт

### Установка зависимостей

```bash
npm install
```

### Разработка

Запустите development сервер:

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

### Сборка

Соберите проект для production:

```bash
npm run build
```

Статические файлы будут сгенерированы в папке `out/`.

## Деплой на GitHub Pages

### Настройка

1. Создайте репозиторий на GitHub
2. Убедитесь что в `next.config.mjs` правильно указан `basePath` (название вашего репозитория)
3. Включите GitHub Pages в настройках репозитория (Settings → Pages → Source: gh-pages branch)

### Деплой одним скриптом

```bash
npm run deploy
```

Эта команда:
1. Соберет проект (`npm run build`)
2. Задеплоит статические файлы в ветку `gh-pages`
3. Сайт будет доступен по адресу: `https://[username].github.io/next-ads-sample-1/`

### Первый деплой

При первом деплое может потребоваться несколько минут, чтобы GitHub Pages обработал изменения.

## Рекламные зоны

Проект включает компонент `AdZone` для размещения рекламы:

### Стандартные размеры

- **300x250** - Medium Rectangle (боковая колонка)
- **728x90** - Leaderboard (горизонтальный баннер)
- **160x600** - Wide Skyscraper (боковая колонка)

### Использование

```tsx
import { AdZone } from "@/components/AdZone";

<AdZone width={300} height={250} />
```

### Размещение рекламы

Рекламные зоны размещены в следующих местах:

1. **Hero секция** - 2 зоны 300x250 справа от главной новости
2. **News Grid** - 1 зона 300x250 после 3-й новости
3. **News Grid** - 1 зона 728x90 (leaderboard) после 6-й новости

## Технологии

- **Next.js 16** - React фреймворк
- **React 19** - UI библиотека
- **TypeScript** - Типизация
- **Tailwind CSS** - Стилизация
- **Lucide React** - Иконки

## Customization

### Добавление новых новостей

Отредактируйте файл `lib/mockNews.ts`:

```typescript
export const newsList: NewsItem[] = [
  {
    id: 10,
    title: "Ваш заголовок",
    description: "Описание новости",
    category: "Категория",
    date: "Дата",
    imageUrl: "URL изображения"
  },
  // ...
];
```

### Изменение цветовой схемы

Отредактируйте `tailwind.config.ts`:

```typescript
colors: {
  primary: "#2563eb",    // Основной цвет
  secondary: "#64748b",  // Вторичный цвет
}
```

### Добавление новых рекламных зон

Используйте компонент `AdZone` в любом месте:

```tsx
<AdZone width={300} height={600} className="my-4" />
```

## Лицензия

MIT
